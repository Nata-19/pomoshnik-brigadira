node_modules/
*.db
*.db-journal
*.log
.DS_Store
Thumbs.db
npm-debug.log*
.env
.env.local
.env.*.local
.vscode/
.idea/
.fly/

# Исходные Excel-таблицы инвентаризации (содержат бизнес-данные)
*.xlsx
*.xls

# Реальные данные инвентаризации (хранятся только локально + в Docker-образе при деплое)
inventory.json

# Конфиг парсера — содержит имена Excel-файлов с названием хозяйства и сортов
parse_config.json

# Визуальный режим брейнсторминга — макеты и временные файлы
.superpowers/
